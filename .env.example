# Copy to Vercel Environment Variables (Production + Preview).
# This site does not use accounts or a database.
VITE_AUTH_ENABLED=false

# Optional. Custom domain for share cards, e.g. grokbot.000666.best
# Leave empty to let the request Host fill in (not *.vercel.app).
# VITE_PUBLIC_HOSTNAME=grokbot.000666.best
